route:
  receiver: operations
  group_by:
    - alertname
    - service
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: operations
    webhook_configs:
      - url: http://127.0.0.1:9099/alerts
        send_resolved: true